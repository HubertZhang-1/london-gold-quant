# wechat_push.env — 复制为 wechat_push.env 并填入你的真实密钥。
# 真实密钥绝不提交到 git（.gitignore 忽略 *.env / .env.*）。
#
# 方式一（推荐）：企业微信群机器人
# 手机微信里建群 → 右上角「…」→「群机器人」→「添加机器人」→ 复制 Webhook 地址：
WECOM_WEBHOOK=https://qyapi.weixin.qq.com/cgi-bin/webhook/send?key=请替换这里是真实key

# 方式二：Server酱（可选备用）
# https://sct.ftqq.com 用 GitHub 登录，得到 SendKey（以 SCT 开头）
#SERVERCHAN_KEY=SCTxxxxxxxxx

# 方式三：PushPlus（可选备用）
# https://www.pushplus.plus 微信扫码登录得到 token
#PUSHPLUS_TOKEN=xxxxxxxx

# 指定使用哪个渠道：wecom | serverchan | pushplus（默认 wecom）
WECHAT_PROVIDER=wecom
